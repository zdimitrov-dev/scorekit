# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
# Direct Postgres connection string (used to apply db/schema.sql)
SUPABASE_DB_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# YouTube Data API (Phase 1)
YOUTUBE_API_KEY=

# IMSLP (Phase 2) — inert until enabled. Confirm IMSLP's terms of use first, then
# set to 1/true. No API key needed (public MediaWiki API).
IMSLP_ENABLED=

# MuseScore (Phase 3) via the Tavily search API, restricted to musescore.com.
# Inert until set. Get a key at tavily.com (free tier ~1,000 searches/month).
# (Google Custom Search was the original plan but is closed to new projects.)
TAVILY_API_KEY=
